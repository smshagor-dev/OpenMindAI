# Android Phase 1 Native Fixes

Native Android validation is intentionally being used as the source of truth for platform compatibility. Desktop-only AI runtimes are separated from the Phase 1 Android shell rather than forcing optional CPU instructions or system libraries onto all phones.

See `MOBILE_NATIVE_BLOCKERS.md` and `ANDROID_PHASE1_STATUS.md` for the current build findings and next gates.
